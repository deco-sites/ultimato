import type { Props } from "deco-sites/ultimato/components/Blocks/ImageSlider.tsx";
import Component from "deco-sites/ultimato/components/Blocks/ImageSlider.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
